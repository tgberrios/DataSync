// Helper to create a test server instance
import express from "express";

export function createApp() {
  // This would need to be implemented to export the Express app
  // For now, we'll need to modify server.js to export the app
  return express();
}
